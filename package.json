{
  "name": "duckdb-sdk",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "ts-node ./src/index.ts serve --configuration=configuration.json",
    "start-server": "ts-node ./src/index.ts configuration serve",
    "test": "jest",
    "generate-config": "ts-node generate-config",
    "import-data": "ts-node import-data"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2",
    "typescript-json-schema": "^0.62.0"
  },
  "dependencies": {
    "@hasura/ndc-sdk-typescript": "^1.2.3",
    "duckdb": "^0.9.2",
    "sql-formatter": "^13.0.4",
    "sqlstring-sqlite": "^0.1.1"
  }
}
